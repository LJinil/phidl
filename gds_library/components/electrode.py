from phidl import Device
import phidl.geometry as pg


def electrode(
    width: float = 1.0,
    length: float = 10.0,
    # E_gap: float = 5.0,
    # E_width: float = 10.0,
    # E_pad_leg: float = 190.0,
    # E_padx: float = 100.0,
    # E_pady: float = 100.0,
    layer: int = 1,
) -> Device:

    return None
